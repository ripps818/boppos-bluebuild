name: bluebuild
on:
  schedule:
    - cron: "00 04 * * *"
  push:
    branches:
      - main        # 🛑 Only auto-build on push if it's the main branch
    paths-ignore:
      - "**.md"
  pull_request:
    branches:
      - main        # 🛑 Only build PRs that target main
  workflow_dispatch: # ✅ Always allows manual clicks on any branch

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  bluebuild:
    name: Build Custom Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        recipe:
          - recipe.yml
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Retrieve Signing Key
        shell: bash
        run: |
          # Create the key file
          echo "${{ secrets.SECURE_BOOT_KEY }}" > boppos.priv
          
          # 🛑 CRITICAL FIX: Tell Docker NOT to ignore this file
          echo "!boppos.priv" >> .dockerignore
          
          # (Optional) Verify it exists for the runner (Debug)
          ls -l boppos.priv
          
      - name: Build Custom Image
        uses: blue-build/github-action@v1.11
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          push: ${{ github.ref == 'refs/heads/main' }}
          skip_checkout: true
          maximize_build_space: true
          rechunk: true
